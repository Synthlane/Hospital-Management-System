version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: medivyx-postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: abcdEF@12
      POSTGRES_DB: synthhms
    volumes:
      - medivyx_pgdata:/var/lib/postgresql/data
      - ./postgres/postgres.conf:/usr/local/etc/postgres/postgres.conf
    command: postgres -c config_file=/usr/local/etc/postgres/postgres.conf
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U admin']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    container_name: medivyx-redis
    restart: always
    command: redis-server --requirepass medplum
    ports:
      - '6379:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', '-a', 'medplum', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: medivyx-backend
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - '8103:8103'
    volumes:
      - ./medplum.config.docker.json:/usr/src/medivyx/packages/server/medplum.config.json
      - medivyx_binary:/usr/src/medivyx/binary
    environment:
      MEDPLUM_PORT: 8103
      MEDPLUM_BASE_URL: 'http://localhost:8103'
      MEDPLUM_APP_BASE_URL: 'http://localhost:3000'
      MEDPLUM_STORAGE_BASE_URL: 'http://localhost:8103/storage'
      MEDPLUM_DATABASE_HOST: postgres
      MEDPLUM_DATABASE_PORT: 5432
      MEDPLUM_DATABASE_DBNAME: synthhms
      MEDPLUM_DATABASE_USERNAME: admin
      MEDPLUM_DATABASE_PASSWORD: abcdEF@12
      MEDPLUM_REDIS_HOST: redis
      MEDPLUM_REDIS_PORT: 6379
      MEDPLUM_REDIS_PASSWORD: medplum
      MEDPLUM_ALLOWED_ORIGINS: '*'
      MEDPLUM_INTROSPECTION_ENABLED: 'true'

  frontend:
    image: node:20-alpine
    container_name: medivyx-frontend
    working_dir: /app
    volumes:
      - ./packages/app:/app
    command: npm run dev
    ports:
      - '3000:3000'
    depends_on:
      - backend

volumes:
  medivyx_pgdata:
  medivyx_binary:
